# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/your_database_name

# Node environment
NODE_ENV=development

# Optional: API base URL for client-side requests (if needed)
NEXT_PUBLIC_API_URL=http://localhost:3000
